// kv.proto —— 压测载体：内存 KV 的 Get/Set（T47）
syntax = "proto3";
package kv;

message GetRequest  { string key = 1; }
message GetResponse { bool found = 1; string value = 2; }
message SetRequest  { string key = 1; string value = 2; }
message SetResponse { bool ok = 1; }
