[project]
name = "perfacet"
version = "0.1.0"
description = "Perfacet examples: MCP 2026-07-28 mock upstream"
requires-python = ">=3.11"
dependencies = [
  "aiohttp>=3.10",
]

[tool.uv]
package = false
